# Terraform Modules

Reusable Terraform modules will live here when the local resource model is promoted toward AWS environments.

Keep modules provider-neutral where practical, and document LocalStack-only differences in `infra/local`.

