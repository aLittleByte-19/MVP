# Features

Each feature owns its domain components, hooks and local types.

Components in this directory may consume feature hooks, but generated OpenAPI calls should remain behind `src/api/pocApi.ts` and feature hooks.
