import { mkdir, writeFile } from "node:fs/promises";

const readmePath = new URL("../src/api/generated/README.md", import.meta.url);

await mkdir(new URL("../src/api/generated", import.meta.url), { recursive: true });
await writeFile(
  readmePath,
  `# Generated API Client

Files in this directory are generated by Orval from \`openapi/v1/alittlebyte-mvp-api.yaml\`.

Do not edit these files manually. Regenerate them from the repository root with:

\`\`\`bash
make openapi-generate
\`\`\`
`
);
